{
  "name": "make-linkedin-great-again",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@types/chrome": "^0.0.280",
    "@types/jest": "^27.5.2",
    "@types/node": "^16.18.119",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "copy-files": "copyfiles src/**/*.html src/**/*.css manifest.json dist/ -f",
    "build": "tsc --build && npm run copy-files",
    "watch": "tsc --watch",
    "clean": "tsc --build --clean"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "tsc-watch": "^6.2.1",
    "typescript": "^5.7.3"
  }
}
